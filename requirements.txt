# ── Cloud API models (always needed) ─────────────────────────────────────────
gradio>=4.0.0
openai>=1.0.0          # also used by Fireworks AI (OpenAI-compatible endpoint)
google-genai>=1.0.0

# Environment variables
python-dotenv>=1.0.0

# Evaluation metrics (lightweight)
rouge-score>=0.1.2
nltk>=3.8.1

# Output / CLI
rich>=13.7.0
pyyaml>=6.0.1
pandas>=2.2.0
numpy>=1.24.0

# Chinese Simplified ↔ Traditional conversion (for Whisper output)
zhconv>=1.4.3

# ── Audio quality / Speech Clarity + Noise Robustness ────────────────────────
# Required for SNR estimation, speech ratio, clarity_score, and noise augmentation
librosa>=0.10.0
soundfile>=0.12.0

# ── BERTScore (optional — heavy, requires PyTorch) ────────────────────────────
# Uncomment the two lines below only if you want to enable BERTScore evaluation.
# Install CPU-only PyTorch first:  pip install torch --index-url https://download.pytorch.org/whl/cpu
# bert-score>=0.3.13

# ── Local HuggingFace models (optional — GPU recommended) ────────────────────
# Uncomment if you want to run local models (ChatGLM, Qwen-local, etc.)
# torch>=2.1.0
# transformers>=4.40.0
# accelerate>=0.27.0
# sentencepiece>=0.1.99
